name: Teardown Postgres Database

on:
  workflow_dispatch:
    inputs:
      remove_data:
        description: 'Remove database data volume (clean slate)?'
        required: true
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  teardown-postgres:
    runs-on: self-hosted
    
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python and virtual environment
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3-dev libxml2-dev libxslt1-dev python3-lxml
          python3 --version
          python3 -m venv .venv --system-site-packages
          source .venv/bin/activate
          python3 -m pip --version
      - name: Stop PostgreSQL
        run: |
          ENV_FILE=/home/pi/secrets/trading_agent.env
          if [ "${{ inputs.remove_data }}" = "true" ]; then
            sudo docker compose --env-file "$ENV_FILE" -f docker-compose.db.yml down -v
            sudo rm -rf /data/postgres_data
            echo "PostgreSQL stopped and data removed."
          else
            sudo docker compose --env-file "$ENV_FILE" -f docker-compose.db.yml down
            echo "PostgreSQL stopped. Data preserved."
          fi
